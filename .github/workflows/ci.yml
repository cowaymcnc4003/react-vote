name: Our CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vote-project
    steps:
      - uses: actions/checkout@v3
      - run: npm i
      - run: npm run build
